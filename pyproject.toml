[project]
name = "llm-gateway-playbook"
version = "0.1.0"
description = "Battle-tested reliability & performance patterns for production LLM gateways"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
